/*
class my_driver extends uvm_driver#(my_transaction);

	virtual	my_if vwr_if;
	virtual my_ig vrd_if;
	
	`uvm_component_utils(my_driver)

	function new(string name = "my_driver", uvm_component parent= null);
		super.new(name,parent);
	endfunction

	virtual function void build_phase(uvm_phase phase)
		super.build_phase(phase);
		if(!uvm_config_db#(virtual my_if)::get(this,"","vwr_if",vwr_if))
			`uvm_fatal("my_driver","virtual interface for write must be configured for wr_if")
		if(!uvm_config_db#(virtual my_if)::get(this,"","vrd_if",vrd_if))
			`uvm_fatal("my_driver","virtual interface for reading must be configured for rd_if")
	endfunction

endclass

task my_driver::main_phase(uvm_phase phase)
	
endtask
*/
